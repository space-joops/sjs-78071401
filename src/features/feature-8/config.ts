import type { FeatureConfig } from "../types";

const config: FeatureConfig = {
  id: 8,
  title: "줍스 궤도 관제",
  description: "궤도를 도는 우주 다마고치 줍스 — 방치·상공 액션·치료 MVP",
  icon: "🛰️",
};

export default config;
